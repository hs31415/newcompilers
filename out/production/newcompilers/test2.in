test2=function(double D, integer K, double rc){
     integer a[100];
     double b[43][20];
     char s = "a string 12;
     if(N==1) return(0);
     integer KL=floor(K * rc);         #split N

     if (KL > .5*K && D >= 1)
            KL = 1&;
     else if(KL <= 1 || KL > (K-1))
            KL = ceiling(KL / 2.0)

     D = K - KL

     integer NL = ceiling(N * KL / K)      #split N
     integer NR = N - NL

     return ( 2+(NL * test2(KL, NL, rc) + NR * test2(D, NR, rc )/N))
}
